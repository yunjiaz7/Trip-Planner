# 注意：本项目需要 Python 3.10 或更高版本
# 如果使用 Python 3.9，某些包（如 pydantic 2.x）可能无法安装

# LangChain框架 (迁移后使用)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-core>=0.1.0

# HelloAgents框架 (保留用于兼容，后续可移除)
# hello-agents[protocols]>=0.2.4

# FastAPI和相关依赖
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP客户端
httpx>=0.27.0
aiohttp>=3.10.0

# 环境变量管理
python-dotenv>=1.0.0

# CORS支持
python-multipart>=0.0.9

# 日志
loguru>=0.7.0

# MCP相关
# 注意：uv 是命令行工具，需要通过系统包管理器安装，不是Python包
# 安装方法：curl -LsSf https://astral.sh/uv/install.sh | sh
# fastmcp 包不存在，已移除

# 其他工具
python-dateutil>=2.8.2

# HelloAgents 评估模块依赖 (保留用于兼容)
huggingface_hub>=0.19.0

